version: '3'

services:
  nginx:
    build: ./nginx
    ports:
      - 80:80
    volumes:
      - ./dist:/dist:ro
    links:
      - apiserver
  apiserver:
    build: ./apiserver
    volumes:
      - ./dist:/app/dist:ro
      - ./data:/app/data:ro
