version: '3'

services:
  apiserver:
    build: ./apiserver
    volumes:
      - ./data:/app/data
  nginx:
    build: ./nginx
    ports:
      - 80:80
    volumes:
      - ./dist:/dist:ro
      - ./data/databases/gallery:/dist/gallery
      - ./data/logs:/var/log/nginx
    links:
      - apiserver